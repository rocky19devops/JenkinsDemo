pipeline {
  agent any

  stages {
    stage('Checkout') {
      steps {
        sh 'echo passed'
        //git branch: 'main', url: 'git@github.com:rocky19devops/JenkinsDemo.git'
      }
    }
    stage('Build and Test') {
      steps {
        sh 'ls -ltrh'
      }
    }
}
