pipeline {
  agent any
  stages {
    stage('Checkout') {
      steps {
        sh 'echo passed'
        //git branch: 'main', url: 'git@github.com:rocky19devops/JenkinsDemo.git'
      }
    }
    stage('Test contents') {
      steps {
        sh 'ls -ltrh'
      }
    }
    stage('build') {
      steps {
        sh 'cd newapp/newapp'
        sh 'docker build -t rockynewapps:latest .'
        sh 'docker images'
      }
    }
  }
}
